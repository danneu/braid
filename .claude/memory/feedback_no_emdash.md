---
name: no emdash
description: Don't use em dash characters in docs or code — use double hyphens instead
type: feedback
---

Don't use em dash (`—`) characters. Use double hyphens (`--`) instead.

**Why:** User preference for consistency in this project's docs.

**How to apply:** When writing or editing documentation and error messages, always use `--` rather than `—`.
